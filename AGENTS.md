# AGENTS.md

## Project goal

日本の開発者・インフラ担当・情シス向けに、ソフトウェアのEOLとサポート期限を「判断しやすい日本語」で提示する。

企画の基準点は `docs/PROJECT.md`、重要な判断理由は `docs/DECISIONS.md`、現在地は `docs/CURRENT_STATE.md`、中期の実行順序は `ROADMAP.md` とする。

## Session start / handoff

新しいチャットや作業セッションでは、過去チャットの記憶を前提にせず、次の順で現在地を復元する。

1. GitHubで最新 `main` と open PR / open Issue を確認する。コミットSHAを過去チャットから使い回さない。
2. `README.md` とこの `AGENTS.md` を読む。
3. `docs/PROJECT.md` で目的、対象ユーザー、優先順位、制約、非目標を確認する。
4. `docs/DECISIONS.md` で長期的に効く重要判断と理由を確認する。
5. `docs/CURRENT_STATE.md` で現在フェーズ、直近の検証結果、次の1〜3件、ブロッカーを確認する。
6. `ROADMAP.md` で中期の順序と着手条件を確認する。
7. アーキテクチャ変更なら `docs/ARCHITECTURE.md`、本番運用・障害対応なら `docs/RUNBOOK.md` を追加で確認する。

`docs/PROJECT_HANDOFF.md` は旧handoff文書との互換導線として残すが、現在地の正本にはしない。

チャットが切り替わっても復元できるよう、目的・判断・現在地が変わるPRでは必要な正本文書も同じPRで更新する。日次データ同期のSHAや一時的なCI run番号など、すぐ古くなる情報は固定しない。

## Decision authority

判断は `PROCEED / INFORM / ASK` の3段階で扱う。

### PROCEED

次を満たす低リスクで可逆な判断は、事前質問を増やさずAI側で進める。

- 記録済みの目的、優先順位、仕様、制約の範囲内
- 継続費用を増やさない
- 公開仕様や中核UXを大きく変えない
- セキュリティ、プライバシー、法的リスクを増やさない
- 容易に変更・取り消しできる
- 既存の品質基準を下げない

例: 内部リファクタリング、既存方針に沿う命名、必要なtest追加、文書同期、明らかな不具合修正の実装詳細。

### INFORM

事前承認までは不要だが、レビュー時に明示する判断。

例: 外部仕様を保つ内部アーキテクチャ変更、無料依存の追加、test / CI構成の意味のある変更、将来の保守方法に影響する方式選択。

必要に応じてPull Requestや `docs/DECISIONS.md` に理由を残す。

### ASK

次はユーザーの明示的な判断・承認を得るまで実行しない。

- プロジェクトの目的、対象ユーザー、提供価値、主要優先順位を変える
- 中核UXや公開仕様に大きなトレードオフがある
- 継続費用、有料サービス、新しい重い外部依存が発生する
- データ削除、破壊的migration等の取り消しにくい変更
- 認証、個人情報、機密情報、権限、公開範囲を変える
- セキュリティ、プライバシー、法的リスク方針を変える
- ドメイン、本番環境等の重要な外部設定を変える
- 合意済み品質gateを下げる
- `REVISE` / `STOP` によりプロダクト方針を大きく転換する
- Pull Requestをマージする

技術的な選択肢が複数あるだけでは質問しない。既存目的・制約から合理的に選べる場合はAI側で選ぶ。`ASK` が必要な場合は推奨案、主要な代替案、重要な差を示す。

## Work selection

次の作業は原則として次の順で優先する。

1. 本番障害、ブロッカー、失敗中の品質gate
2. 後続の成立性を左右する重大な不確実性・技術リスク
3. 現在の中核価値や検証仮説に直接つながる最小の実装
4. 安全に届けるために必要な品質・運用・基盤作業
5. 本番データや検証結果から必要性が確認できた改善
6. 便利機能、将来拡張、見栄えだけの任意項目

ユーザーが明示した優先順位や期限がある場合はそれを優先する。

1つのPull Requestは原則1つの明確な目的に絞る。ファイル種別ではなく、目的・完了条件・検証方法・取り消し単位で分割する。

`docs/CURRENT_STATE.md` の「次に行うこと」は優先順の1〜3件に保ち、長いバックログにしない。今すぐ着手しないが継続追跡が必要な独立作業だけIssue化する。

Pull Requestのマージ後は古い作業リストを機械的に続けず、直近の検証結果、本番状態、現在フェーズから次の1件を選び直す。

## Repository change workflow

- `main` は本番ブランチ。変更前に必ず最新 `main` と直前の関連PRがマージ済みか確認する。
- 通常の変更は最新 `main` から作業ブランチを作成し、GitHub上で変更、PR作成、diff確認、必要なCI完走確認まで行う。
- このプロジェクトのChatGPT作業では Codex を使わず、GitHub integrationを直接利用する。
- PR説明、コミットメッセージ、プロジェクト文書は原則日本語で書く。コードコメントは必要な箇所だけ簡潔な日本語にする。
- CIが成功し、`main` からbehindしておらず、mergeableであることを確認してマージ可否を報告する。
- ユーザーから明示的な指示がない限りPRをマージしない。
- 運用費をほぼ0円に保つ。新しいWorker / KV / DB / 外部API・有料サービス依存は、明確な必要性と再設計判断なしに追加しない。

## Validation loop

1回の作業では「この変更で何を確認するか」を1つの目的として説明できる状態にする。

実装・変更後は次のいずれかを明示する。

- `PASS`: 予定した検証を満たし、次へ進める
- `FIX`: 方針は維持できるが、同じ目的の範囲で修正が必要
- `REVISE`: 設計や仮説を見直す必要がある
- `STOP`: 現在の方向への追加実装を止めるべき

`FIX` の未解決事項や失敗した品質gateを放置して次の機能へ進まない。事前に決めたgateを、結果が悪いという理由だけで後から緩めない。

技術探索では、採用条件、不採用条件、次候補へ進む条件、探索終了条件を事前に決める。

## Release / feedback loop

マージやデプロイ成功だけを完了扱いにしない。変更内容に応じて本番環境で主要経路、静的アセット、ブラウザ挙動、SEO設定、計測状態を確認する。

公開後の判断は次で整理する。

- `CONTINUE`: 仮説が概ね成立している
- `OPTIMIZE`: 価値は確認できるが改善余地がある
- `REVISE`: 主要仮説の見直しが必要
- `STOP`: 現在の方向への追加投資を止める
- `未判定`: 観測期間や母数が不足している

本番で重大な問題が見つかった場合は、通常は新機能より修正を優先する。重要な学びは `PROJECT / DECISIONS / CURRENT_STATE` の適切な正本へ戻す。

意味のある節目（大きな公開、技術探索終了、重大障害、方向転換、停止等）では必要に応じて振り返りを行う。毎PRで形式的な振り返りは行わない。

## Non-negotiable architecture

1. Astro + TypeScript の静的サイトを基本とする。公開コンテンツをSSRやDBへ安易に移行しない。
2. Cloudflare Workers + Static Assets を配信基盤とし、Workerは通知API・公開API・badge・scheduled処理など必要な動的機能だけを担当する。
3. ライフサイクルデータの主ソースは endoflife.date API v1 とする。HTMLスクレイピングは禁止。
4. 本番build時にendoflife.date APIを必須依存させない。コミット済み `src/data/eol-snapshot.json` からbuildする。
5. API取得・正規化は `scripts/sync-eol.mjs` に集約し、API仕様変更はここで吸収する。
6. endoflife.dateの製品説明文を機械翻訳して転載しない。日本語説明は `src/data/product-meta.ts` 等で独自作成する。
7. 元データ、公式サポート方針、免責事項への導線を削除しない。
8. アカウント・恒久DB・private GitHub認証など、運用コストや個人情報管理を増やす設計は明確な再設計判断なしに導入しない。

## Privacy / local state

- マイEOL、閲覧履歴、GitHub importの保存結果は原則ブラウザlocalStorageへ保持する。
- GitHub importは公開リポジトリを対象とし、GitHub tokenを入力・保存させない。
- ブラウザ内で完結できる処理をWorker/KVへ不要に送信しない。
- 通知機能で必要な外部送信先・メール情報は、機能提供に必要な最小範囲だけ保存する。

## UX rules

- ISO日付だけで終わらせず「EOLまであとN日」を併記する。
- 0–30日: critical、31–90日: warning、91–180日: planning、181日以上: supported。
- EOL済みを明確に区別する。
- 「アップグレード推奨」は断定せず、公式移行ガイド・互換性確認を促す。
- モバイルで期限が読みやすいことを優先する。
- localStorage、GitHub API、通知APIなど外部依存が失敗しても、基本的なEOL情報の閲覧を妨げない。
- 比較・一覧が主目的の画面は高密度テーブルを基本とし、探索・説明・確認操作が主目的の箇所はカードを使い分ける。
- 外部 `http` / `https` リンクは別タブ、内部リンクは同一タブを基本とする。

## SEO rules

- 製品ページのcanonicalは `/eol/{slug}/`。
- タイトルの基本形は `{製品名}のEOL・サポート終了日 | EOL情報.jp`。
- 主要検索意図: `{product} EOL`, `{product} サポート期限`, `{product} {version} EOL`。
- sitemap、RSS、iCalendarを壊さない。
- indexableなcanonical HTMLとsitemapの整合性を維持する。
- 薄い自動生成記事を大量作成しない。製品ページに独自価値を追加する。
- title / description の個別最適化、内部リンク拡張、バージョン専用URLは、Search Console等の実需要を根拠に行う。
- Search Consoleの反映・実データ待ちの間は、インデックス登録を狙った推測変更を繰り返さない。

## Data integrity / trust

- 日付は `YYYY-MM-DD` を正規形とする。
- 不明な日付を推測しない。nullは「未定」と表示する。
- EOL期限の変更を `change-log.json` に記録する。
- 同期差分の監査情報を `audit-log.json` に残す。
- API schema versionをsnapshotに保存する。
- 新しいAPIフィールドをUIへ直接漏らさず、正規化層を通す。
- 公式ソース照合、商用サポート、リリース変更点など手動確認データには確認日を残し、将来の再確認対象にできるようにする。

## Testing before merge

変更内容に応じて必要な検証を選ぶ。コード・データ・buildへ影響する通常変更では少なくとも次を確認する。

- `npm run check`
- `npm run build`
- 関連する `npm run test:*`
- `/`, `/eol/`, `/upcoming/`, `/calendar/`, 主要製品ページが生成されること
- sitemap.xml / feed.xml / calendar.ics が生成されること
- EOL日境界（昨日/今日/30/90/180日）
- UIの重要フローを変更した場合は、localStorage無効・外部API失敗時のフォールバック
- 既存CIのperformance budget、browser smoke、SEO validationを安易に緩和しない

すべてのPRで儀式的に同じ重い検証を増やさない。docs-only等の低リスク変更は、内容に見合う検証へ絞ってよいが、CI方針変更自体は別PRで検証する。

## Current priorities

現在地の正本は `docs/CURRENT_STATE.md` とする。Phase 0〜6は完了済みで、現在はSearch Consoleのインデックス反映・検索実データを待つGrowth gate期間。

企画・判断・現在地の標準化と文書専用変更のCI軽量化は導入済み。今回の区切りの振り返りと軽量CIの完了条件は `docs/PROCESS_REVIEW_2026-09.md` を参照し、最終確認後は同じ整備作業を繰り返さない。検索実データがない段階では、運用維持、バグ修正、ユーザーから明示された小規模UX改善を優先する。本番障害・資格情報更新時は `docs/RUNBOOK.md` を参照する。
